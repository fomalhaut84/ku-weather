{
  "name": "@ku-weather/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "echo 'Phase 7에서 구현' && exit 1",
    "dev": "echo 'Phase 7에서 구현' && exit 1",
    "lint": "echo 'Phase 7에서 구현' && exit 1"
  },
  "dependencies": {
    "@ku-weather/shared": "workspace:*"
  }
}
