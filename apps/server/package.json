{
  "name": "@ku-weather/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "echo 'Phase 2에서 구현' && exit 1",
    "dev": "echo 'Phase 2에서 구현' && exit 1",
    "test": "echo 'Phase 2에서 구현' && exit 1"
  },
  "dependencies": {
    "@ku-weather/shared": "workspace:*"
  }
}
